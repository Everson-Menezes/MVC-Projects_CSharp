@model OBM_Project.Models.ViewModels.CadastrarOrcamentoViewModel


@{
    ViewData["Title"] = "Orçamento";
}
<table style="width:100%">
    <tr>
        <td style="width:320px">
            <div>
                <form asp-action="SolicitarOrcamento" asp-controller="Orcamento">
                    <div class="form-group">
                        <label for="exampleFormControlSelect1">Tipo Serviço</label>
                        <select asp-for="Orcamentos.TipoServicoId" asp-items="@(new SelectList(Model.TipoServicos, "Id", "Nome"))" class="form-control">
                            <option>selecione</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="exampleFormControlSelect1">Subtipo Serviço</label>
                        <select asp-for="Orcamentos.SubTipoServicoId" asp-items="@(new SelectList(Model.SubTipoServicos,"Id", "Nome"))" class="form-control">
                            <option>Selecione uma opção</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="exampleFormControlSelect1">Qual a urgência do serviço</label>
                        <select asp-for="Orcamentos.NecessidadeId" asp-items="@(new SelectList(Model.Necessidades, "Id", "Nome"))" class="form-control">
                            <option>Selecione uma opção</option>
                        </select>
                    </div>
                    <label for="FormControlTextarea">Observações</label>
                    <div class="form-group" >
                        <textarea asp-for="Orcamentos.Observacao" placeholder="Escreve seu texto aqui!" style="height:120px; width:400px; resize:none" class="form-control"></textarea>
                        <br /><br />
                        <input type="submit" value="Enviar" class="btn btn-default" />
                    </div>
                </form>
            </div>
        </td>
        <td>
        </td>
        <td>
            
        </td>
    </tr>
</table>


@section A{ }
@section B{ }
@section C{ }
@section D{ }

@section F{ }
@section G{ }

@section I{ }
